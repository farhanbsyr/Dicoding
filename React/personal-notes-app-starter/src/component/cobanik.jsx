import React from "react";
import { getNote } from "../utils/local-data";

class Cobanik extends React.Component {
  constructor(props) {
    super(props);

    this.state = {
      cuks: notes,
    };
  }
}
